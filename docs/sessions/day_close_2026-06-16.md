# Day Close — 2026-06-16

End-of-session report for InjexCore. Branch: `refactor/v1-architecture-stabilization`.

This note closes a 14-day arc since the last session note ([day_close_2026-06-02.md](day_close_2026-06-02.md)).
No closing artifacts were produced in between; this report consolidates the whole span.

---

## 1. Session Summary

Since 2026-06-02 the **Intelligence Layer** grew from a single component (Iteration A —
Behaviour Intelligence) into a complete **9-component stack**, gained two external
**context layers**, a read-only **dashboard consumption contract**, and a quality
**hardening pass**. The three-layer boundary first codified in v2
(`preprocessing` → `intelligence` → planned `models`) is now densely populated on the
intelligence side, with `src/context/` added as an analytical-only sibling. Everything is
committed and pushed; the working tree is clean.

Shipped, grouped by the five development arcs (commit hashes inline):

- **Iteration B — relationship / statistical scoring** (`e5d075c`, `d381f4b`, `2926fda`,
  `84d432f`, `83a106d`, `450d52b`, `fc47218`, `841554a` — 2026-06-12):
  - `src/intelligence/_common/` — shared run utilities: run-versioning (`runs.py`:
    timestamp run-ids, manifest-as-completion-marker, `latest` resolution), structural
    fingerprint, behaviour-artifact loaders + the strict `align_labels` leakage contract,
    feature selection, joblib persistence, base fit manifest.
  - `src/intelligence/correlation/` — per-profile Pearson + Spearman references on
    behaviour's train window; strongest/redundant-pair reports; train-vs-validation shift
    **diagnostics** (AWARE only, no alerts).
  - `src/intelligence/pca/` — per-profile RobustScaler + PCA (EVR target 0.95), joblib
    models + loadings + per-row T²/Q scores + reconstruction contributions (`is_train`
    preserved); unsupportable profiles skipped + reported.
  - `src/intelligence/anomaly/` — four explainable detectors (statistical vs baselines,
    Mahalanobis/Ledoit-Wolf, PCA T²/Q via `--pca-run`, seeded Isolation Forest),
    train-ECDF normalization, conservative-max combined score, train-percentile severity
    (`normal`/`warning`/`anomaly`, `unscored` for unsupported profiles).
  - `src/intelligence/__main__.py` — registry-based `--component` dispatcher extended for
    all new components; CI fixed to install the `[models]` extra (sklearn).
- **Sensor Health + Context layers** (`9fe675c`, `03e763d` — 2026-06-12):
  - `src/intelligence/sensor_health/` — nine instrumentation-anomaly rule families,
    profile-aware suppression (stopped-state zeros never flag), per-`(timestamp, sensor)`
    status + events (`pending_review`) + quarantine recommendations
    (`approval_required=True, approved=False` — never auto-excludes). Detected the
    `inlet_hopper_points` flatline-zero from 2024-09-17 16:23.
  - `src/context/bom/` — raw BOM CSV → components → aggregated orders with deterministic
    `bom_signature`/`recipe_context_key` → overlap/gap/transition diagnostics →
    master-aligned `bom_context_timeline` → read-only BOM-aware forensic addendum.
  - `src/context/operational/` — master-aligned overlay (profile + derived steam
    conditioning + sensor-health + full BOM columns + deterministic `context_key`),
    context-transition events, context-aware forensic addendum.
- **Drift + Incidents** (`1add492` — 2026-06-14):
  - `src/intelligence/drift/` — windowed PSI/KS/Wasserstein + robust moment shifts vs the
    train reference (global + per profile), multivariate score shifts read from
    anomaly/pca (no refit), sensor-quality-aware classification, raw vs healthy-only
    analytical views (honestly-labeled `healthy_only_proxy`), strong faulty events
    promoted to `sensor_drift`. Confirmed the persistent critical `inlet_hopper_points`
    drift and that ~70% of validation drift mass disappears healthy-only.
  - `src/intelligence/incidents/` — events → incidents (type + family + temporal +
    Jaccard grouping), recurring-pattern collapse, cross-source merge, transparent burst
    suppression, associative relationships (`causality_status=unknown`), quarantine
    actions pending approval, prioritized review pack, drift-aware forensic addendum.
    1,038 candidate events → 88 incidents → 39-row review pack.
- **Iteration C Part 3 — reference + interpretation** (`a71a54f` — 2026-06-15):
  - `src/intelligence/reference/` — immutable `reference_v1` baseline + the
    `inlet_hopper_points` quarantine proposal + three candidate proposals (all
    `pending_review`/`approved=False`) + seeded decision log + conservative residual-drift
    materiality (`material=False` on real data). Proposes, never approves.
  - `src/intelligence/scoring_experiment/` — four interpretive scenarios over persisted
    scores (`baseline_v1`, quarantine-aware suppression, `healthy_only_proxy`,
    reference-candidate diagnostic); original scores never mutated, no refit/rescore.
    Quarantine-aware view suppresses ~91% (30,096 of 33,186 non-normal rows) as
    `inlet_hopper_points`-dominated, leaving a 1,203-anomaly review backlog. Emits the
    decision-report forensic addendum (6-action decision matrix).
- **Hardening + Dashboard contract** (`658cd58`, `28bb902`, `0d89aa7` — 2026-06-16):
  - Hardening sprint — 8 Codex findings fixed with new/updated tests and an independent
    qa-agent PASS ([reports/hardening/post_iteration_c_hardening_report.md](../../reports/hardening/post_iteration_c_hardening_report.md)).
  - Sensor-health manifest creation with latest-behaviour resolution; controlled
    rematerialization of the real-data chain to canonical run
    `remat-v1-20260616T102558Z` / BOM `20260612T124909Z`
    ([reports/hardening/controlled_rematerialization_report.md](../../reports/hardening/controlled_rematerialization_report.md)).
  - `src/dashboard/` — read-only consumption contract (`contract.py` pins the canonical
    chain + component registry) + lineage validator (`lineage.py`'s
    `validate_dashboard_chain` fails closed on missing/wrong-component/wrong-id manifests,
    surfaces non-canonical/stale chains as warnings, never mutates). Consumer-boundary
    `expected_component` enforcement (LINEAGE-01), explicit `behaviour_run_id` provenance
    in correlation/pca/anomaly/sensor-health manifests (PROV-01), operational + BOM
    addendum `--anomaly-run`/`--forensic-run` overrides that fail closed (CTX-01).
    Reports: [reports/dashboard/pre_dashboard_contract_cleanup_report.md](../../reports/dashboard/pre_dashboard_contract_cleanup_report.md).

Design boundary held throughout: the Intelligence Layer fits **descriptive** artifacts
and produces **interpretive** scenarios — it never refits predictive estimators, rescores
with altered feature matrices, approves a quarantine, or excludes a sensor. The context
layers are analytical-only (read-only joins, never model-fitting inputs). `src/models/`
remains the reserved home for predictive scorers.

---

## 2. Repository Readiness

- **Branch:** `refactor/v1-architecture-stabilization` (not `main`).
- **Remote:** up to date with `origin/refactor/v1-architecture-stabilization` — all 15
  feature/chore commits since the v2 snapshot (`2d5534c`) are pushed.
- **Working tree:** clean (`git status` shows nothing to commit).
- **`.gitignore`:** `/data/` ignored — the run-versioned intelligence/context artifacts
  under `data/intelligence/` and `data/context/` are not staged (expected).

**Commit / push:** nothing to do — the user confirmed everything is already committed and
pushed. The artifacts added by this session (this note, `MASTER_VERSION_v3.md`, refreshed
memory files) are local documentation the user can commit at their discretion.

---

## 3. Quality Gates

`pytest` **575 passed** (~55 s; verified collected count via `pytest --collect-only`).
`ruff check` + `ruff format --check` clean · `mypy src` green (lenient baseline, pydantic
plugin enabled). The hardening sprint (`658cd58`) closed 8 Codex findings with an
independent qa-agent PASS; the controlled rematerialization (`28bb902`/`0d89aa7`) rebuilt
the real-data chain end-to-end into the canonical pinned run. Detailed evidence:
[post_iteration_c_hardening_report.md](../../reports/hardening/post_iteration_c_hardening_report.md),
[controlled_rematerialization_report.md](../../reports/hardening/controlled_rematerialization_report.md),
[pre_dashboard_contract_cleanup_report.md](../../reports/dashboard/pre_dashboard_contract_cleanup_report.md).

---

## 4. Documentation / Memory Drift

| Surface | Status |
|---|---|
| `CLAUDE.md` | Up to date — module table covers all 9 intelligence components + both context layers + `src/dashboard/`; Current Status reflects through commit `0d89aa7`; "575 tests" matches the collected count. No edit needed. |
| `README.md`, `docs/README.md` | Up to date — architecture diagram, status, and doc index match the repo. No edit needed. |
| `docs/intelligence/`, `docs/context/`, `docs/dashboard/`, `docs/architecture/` | Current — one reference doc per component/layer (9 intelligence + 2 context + dashboard contract + dataflow). |
| `docs/project-state/MASTER_VERSION_v3.md` | Added this session. |
| Project memory (`memory/`) | Refreshed this session — two project memories were stale (`project_intelligence_layer.md` said "Iteration B next"; `project_controlled_rematerialization.md` flagged the SH None-labels fix + operational CLI overrides as uncommitted — both now landed in `28bb902`/`0d89aa7`). |

Minor open item (carried): the headline "575 tests" includes parametrised expansions (raw
`def test_` count is lower) — not a contradiction, noted for future counts.

---

## 5. Next Session — Decision Review, then the Dashboard MVP

The system is ready for **technical validation dashboard planning, not production
deployment**. Anchor on the deferred, human-gated items from CLAUDE.md:

1. **Human review of the Iteration C Part 3 decision report** — read
   `data/intelligence/forensics/reference_decision/<latest>/decision_summary.md` +
   `decision_matrix.parquet`. Two decisions are pending:
   - the `inlet_hopper_points` quarantine approval (`approval_required=True,
     approved=False`), and
   - the deferred Reference v2 candidate (residual healthy-only drift assessed
     `material=False`, so deferral — not refit — is the current recommendation).
2. **Read-only Technical Validation Dashboard MVP** — the contract + lineage validator
   (`src/dashboard/`, [dashboard_data_contract.md](../dashboard/dashboard_data_contract.md))
   are in place and pinned to `remat-v1-20260616T102558Z` / BOM `20260612T124909Z`; the
   UI / `src/visualization/` is not started. Build the read-only views over the pinned
   canonical chain.
3. **Predictive `src/models/` layer** — Isolation Forest / LOF / Mahalanobis scoring
   against the behaviour baselines + fit manifest, once dashboard planning is settled.

Decisions to make before coding the dashboard: dashboard stack/framework, which pinned
artifacts to surface first (review pack + scoring scenarios are the highest-signal), and
whether an *approved* Reference v2 refit precedes or follows the dashboard MVP (gated on
item 1 + plant-record review).
