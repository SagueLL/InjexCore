# Day Close — 2026-06-28

End-of-session report for InjexCore. Branch: `feature/dashboard-v0-ui`.

This note closes a 12-day arc since the last session note
([day_close_2026-06-16.md](day_close_2026-06-16.md)). The project crossed a boundary in
this span: the backend Intelligence Layer was sealed ("fase 1 — cierre técnico") and the
**frontend Dashboard v0** was started — the first code to live outside `src/`, in a new
`apps/dashboard/` monorepo tree.

---

## 1. Session Summary

Two distinct phases since 2026-06-16:

**A. Backend close-out (2026-06-17, `0810675`)** — closed the post-Iteration-C technical
phase:
- Added the **10th Intelligence-Layer component**, `human-decision`
  (`src/intelligence/human_decisions/`) — a run-versioned, read-only **human decision
  overlay** under the flat `data/intelligence/forensics/human_decisions/<run_id>/` layout.
  It encodes the scoped `inlet_hopper_points` quarantine approval + the Reference-v2
  deferral as decision-support metadata only — mutates nothing, refits nothing, applies no
  quarantine; keeps `artifact_state` / `human_review_state` / `applied_pipeline_state` /
  `dashboard_overlay_state` distinct. Dispatcher now exposes **10 components**.
- Consolidation docs: `docs/project-state/MASTER_VERSION_v3.md`,
  `docs/sessions/day_close_2026-06-16.md`, and two reports —
  [iteration_c_part3_human_decision_review.md](../../reports/decision_reviews/iteration_c_part3_human_decision_review.md)
  and [post_iteration_c_technical_state_product_implications.md](../../reports/technical_state&product_implications/post_iteration_c_technical_state_product_implications.md).
- Suite grew to **590 tests** (+15: 14 overlay + 1 dispatcher param).

**B. Dashboard v0 frontend (2026-06-27 → today)** — the focus of this session. A new
Next.js app scaffolded at `apps/dashboard/` (first non-`src/` workspace; no root
`package.json`, so it stands alone for now):

- **Scaffold + environment** (`2053877`, 06-27): `create-next-app` base, 3 new MCPs, and
  most of the frontend tooling. Stack: **Next.js 16.2.9 (Turbopack), React 19.2.4,
  Tailwind v4, shadcn/ui, Recharts 3.9** (installed, not yet used), `date-fns`,
  `react-day-picker`, hugeicons. Note: `apps/dashboard/AGENTS.md` flags this as a
  non-standard Next.js — agents must read `node_modules/next/dist/docs/` before writing
  Next code.
- **Coding rules** (`4d99d82`): expanded global + frontend coding standards
  (`.claude/rules/code-style.md`, `.claude/rules/frontend/react.md`) defining the v0 scope,
  the `src/` layout contract, the 5 required views, and the industrial-UX language rules.
  *(Further refinements to these two files remain uncommitted — see §2.)*
- **App shell + routes** (`03cf22a`): `app-shell.tsx`, `sidebar-nav.tsx`, `page-header.tsx`
  and the five planned routes — `/overview`, `/timeline`, `/sensor-health`,
  `/drift-anomaly`, `/incidents`.
- **Shared visual components** (`0e33cba`): `src/components/dashboard/` —
  `kpi-card`, `status-badge`, `section-card`, `insight-card`, `empty-state` — plus the
  shadcn/ui primitive set (badge, button, card, table, tabs, select, dialog, sheet,
  popover, dropdown-menu, command, calendar, separator, skeleton, input, textarea,
  input-group, tooltip).
- **Executive Overview view** (`7573afb`, `b635546`, `dbc2006`): the dashboard types
  (`src/types/dashboard.ts`), demo data (`src/lib/dashboard/demo-overview.ts`), navigation
  config (`src/lib/dashboard/navigation.ts`), and the fully-built `/overview` page —
  asset/dataset/period header + operational status badge, KPI grid, operational-insight
  cards, and an honest **Limitations** section. The other four routes are intentional
  placeholders (thin "content added in the next step" pages).
- Architecture note: [InjexCore_Technical_Stack_Dashboard_v0.md](../architecture/InjexCore_Technical_Stack_Dashboard_v0.md).

Design boundary held: the dashboard is **read-only and demo-data-driven** for now — no
backend wiring, no Parquet reads from the frontend, no real-time. `/overview` runs on
static `demo-overview.ts`; the planned data path (Python intelligence → dashboard-ready
JSON → Next.js) is not built yet. This matches the v0 scope in the frontend rules.

---

## 2. Repository Readiness

- **Branch:** `feature/dashboard-v0-ui` (not `main`) — a new feature branch for this
  workstream; the prior session was on `refactor/v1-architecture-stabilization`.
- **Remote:** up to date with `origin/feature/dashboard-v0-ui`; HEAD `dbc2006` matches the
  remote head — **all commits pushed**.
- **Working tree:** **not clean** — two tracked files have uncommitted additions:
  - `.claude/rules/code-style.md` (+146 lines)
  - `.claude/rules/frontend/react.md` (+329 lines)

  These are real-content refinements to the coding standards (475 added lines, 0
  deletions) — the detailed rules that guided today's frontend work, expanded past the
  shorter version committed in `4d99d82`. **Decision for the user:** commit them as a small
  `chore(rules): refine dashboard coding standards`, or leave as local. Low risk either way;
  they are documentation, not application code.
- **`.gitignore`:** `apps/dashboard/node_modules/` and `.next/` are ignored (verified — the
  build artifact and deps are not staged).

---

## 3. Quality Gates

This was a **frontend day**, so the gates are the dashboard's, not Python's:

| Gate | Result |
|---|---|
| `npm run lint` (eslint) | **clean** — no findings |
| `npx tsc --noEmit` | **clean** (exit 0) — strict TS, no errors |
| `npm run build` (`next build`) | **✓ pass** — Next.js 16.2.9 (Turbopack), compiled in 3.9 s, TypeScript checked, **9/9 static pages** generated; all routes prerendered static (`/`, `/_not-found`, `/overview`, `/timeline`, `/sensor-health`, `/drift-anomaly`, `/incidents`) |

The Python suite (**590 tests**) was **not re-run** — no Python code changed since
`0810675` (06-17), where it last passed. The dashboard has **no frontend tests yet**
(none expected at v0 scope).

---

## 4. Documentation / Memory Drift

The repo moved ahead of `CLAUDE.md` during the close-out + frontend phases. **`CLAUDE.md`
is now stale** in four places (flagged, not auto-edited — recommend a single follow-up edit):

| Surface | Status |
|---|---|
| `CLAUDE.md` — component count | **STALE.** Says "**9-component stack**"; the layer is now **10 components** (`human-decision` added 06-17). The Intelligence-Layer command block and dispatcher line (`--component behaviour\|...\|scoring-experiment`) omit `human-decision`. |
| `CLAUDE.md` — test count | **STALE.** Says "**575 tests**"; suite is now **590**. |
| `CLAUDE.md` — `src/dashboard/` row | **STALE.** Says "The dashboard **UI** is not started." The UI **is** started — `apps/dashboard/` (Next.js, `/overview` built, build green). |
| `CLAUDE.md` — module table + Current Status | **INCOMPLETE.** No row for `src/intelligence/human_decisions/`; no mention of `apps/dashboard/` or the Dashboard v0 frontend workstream anywhere. |
| `README.md`, `docs/README.md` | Not re-checked this session; likely carry the same "intelligence-only" framing. Worth a pass when `CLAUDE.md` is updated. |
| `apps/dashboard/README.md` | Default `create-next-app` boilerplate (references `app/page.tsx`; project uses `src/app/`). Cosmetic; low priority. |
| Project memory (`memory/`) | **Refreshed this session** — `project_intelligence_layer.md` (human-decision now committed in `0810675`; dashboard UI now *started*, not "next") and a new `project_dashboard_v0.md` capturing the frontend MVP state + the `apps/` monorepo decision. |

---

## 5. Next Session — Build out the Dashboard v0 views

Foundation is solid and demo-ready for `/overview`. Highest-value next steps:

1. **Wire the data contract to the UI.** The read-only Python contract
   (`src/dashboard/`, pinned to `remat-v1-20260616T102558Z` / BOM `20260612T124909Z`) and
   the new `human-decision` overlay are the source of truth. Decide the export format
   (intelligence → dashboard-ready JSON) and replace `demo-overview.ts` with it for
   `/overview` first. Highest-signal artifacts to surface: the incidents **review pack** and
   the **scoring-experiment scenarios**.
2. **Fill the four placeholder views** — `/timeline`, `/sensor-health`, `/drift-anomaly`,
   `/incidents` — each over its pinned canonical artifacts. Recharts is installed; charts
   must live in dedicated `"use client"` components, not in route pages (per the frontend
   rules). `/sensor-health` and `/incidents` carry the `inlet_hopper_points` story end-to-end.
3. **Refresh `CLAUDE.md`** (the §4 drift) — single edit: 9→10 components + `human-decision`
   in the command/dispatcher blocks, 575→590 tests, the `src/dashboard/` row + an
   `apps/dashboard/` entry, and a Current-Status line for Dashboard v0.
4. **Commit the rules refinements** (§2) if keeping them.

Still deferred (gated, unchanged): an *approved* Reference v2 refit / true PCA-Mahalanobis
rescore (pending plant records), and the predictive `src/models/` layer (IF / LOF / OCSVM /
Mahalanobis against the behaviour baselines).
