"""Operational Context Overlay — master-aligned interpretation layer.

Combines, for every master timestamp: the Behaviour profile, a derived
steam-conditioning context, the Sensor Health context and the BOM context
into one separate timeline plus context-transition events and a read-only
context-aware forensic addendum. Enriches interpretation only: it never
modifies the master dataset, never refits Intelligence-Layer models and
never changes thresholds.
"""
